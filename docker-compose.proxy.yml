version: '3.8'

services:
  betrivers:
    environment:
      - PROXY_URL=${PROXY_URL}
      - HTTP_PROXY=${PROXY_URL}
      - HTTPS_PROXY=${PROXY_URL}
      - NO_PROXY=localhost,127.0.0.1,normalizer,redis,bovada,metrics-proxy

  barstool:
    environment:
      - PROXY_URL=${PROXY_URL}
      - HTTP_PROXY=${PROXY_URL}
      - HTTPS_PROXY=${PROXY_URL}
      - NO_PROXY=localhost,127.0.0.1,normalizer,redis,bovada,metrics-proxy

  caesars:
    environment:
      - PROXY_URL=${PROXY_URL}
      - HTTP_PROXY=${PROXY_URL}
      - HTTPS_PROXY=${PROXY_URL}
      - NO_PROXY=localhost,127.0.0.1,normalizer,redis,bovada,metrics-proxy

  sugarhouse:
    environment:
      - PROXY_URL=${PROXY_URL}
      - HTTP_PROXY=${PROXY_URL}
      - HTTPS_PROXY=${PROXY_URL}
      - NO_PROXY=localhost,127.0.0.1,normalizer,redis,bovada,metrics-proxy

  unibet:
    environment:
      - PROXY_URL=${PROXY_URL}
      - HTTP_PROXY=${PROXY_URL}
      - HTTPS_PROXY=${PROXY_URL}
      - NO_PROXY=localhost,127.0.0.1,normalizer,redis,bovada,metrics-proxy