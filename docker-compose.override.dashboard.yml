services:
  dashboard:
    image: nginx:alpine
    container_name: oddsops-dashboard
    ports: ["8090:80"]
    volumes:
      - ./web/dashboard-mini:/usr/share/nginx/html:ro
    restart: unless-stopped
